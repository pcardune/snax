import { ParsingExample } from '../../components/editor/ASTViewer.js';

# Parsing

Snax uses a [Parsing Expression Grammar](https://en.wikipedia.org/wiki/Parsing_expression_grammar) to define it's language. You can explore the
grammar using the below tool.

<ParsingExample>
{`
import io from "snax/io.snx"
let a = 1;
`}
</ParsingExample>