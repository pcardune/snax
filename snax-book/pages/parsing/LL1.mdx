import { LL1TableUI } from '../../components/LL1TableUI';
import { GrammarTable } from '../../components/GrammarTable';
import expressionSpec from '../../grammars/expressionSpec';
import numbersSpec from '../../grammars/numbersSpec';
import { LL1ParserUI } from '../../components/LL1ParserUI';

# Table-Driven LL(1) Parsing

If we have a backtrack free grammar, we can use a 1 token lookahead
to decide which production to expand next. The first step of generating
an LL(1) parser is to create the lookup table that maps a (non-terminal, terminal)
pair to a particular production.

Given the following expression grammar:

<GrammarTable grammar={expressionSpec} />

The LL(1) lookahead table would look like this:

<LL1TableUI start="Goal" grammarSpec={expressionSpec} />

## Parsing

<LL1ParserUI grammar={expressionSpec} start="Goal" input="(num)+name" />

Let's look at an example of parsing a binary number. Below is the grammar we'll use, along with the lookahead table:

<div
  style={{
    display: 'flex',
    flexFlow: 'row wrap',
    justifyContent: 'space-around',
  }}
>
  <GrammarTable grammar={numbersSpec} />
  <LL1TableUI start="Goal" grammarSpec={numbersSpec} />
</div>

We'll parse the string "101". As the parsing algorithm unfolds, the parser goes through these states:

<LL1ParserUI grammar={numbersSpec} start="Goal" input="101" />
