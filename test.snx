import io from "snax/io.snx"

func main() {
  io::printStr(@"hello world\n");
  let meow = "meowwww\n";
  meow.buffer[1] = 'i';
  io::printStr(@meow);
  io::printStr(@"more snax please!\n");
  io::printStr(@"type something: ");
  let input = io::readStr(100);
  io::printStr(@input);
}