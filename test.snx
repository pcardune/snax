func sumTo10(i:i32, stepSize:i32) {
  let n = 0;
  while (i > 0) {
    n = n+stepSize;
    i = i-1;
  }
  return n;
}
sumTo10(300, 3);

let s:&u8 = "yello world\n";

let iovec:&i32 = 400;
iovec[0] = 0; // this happens to be where s is stored...
iovec[1] = 12;

wasi_fd_write(
  1, // stdout
  400, // pointer to io vector
  1, // length of io vector
  20 // where to store number of bytes written
);
